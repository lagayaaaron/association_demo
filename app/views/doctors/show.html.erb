<div class="container p-6 mx-auto">
  <h1 class="mb-4 text-3xl font-bold text-gray-800">Doctor: <%= @doctor.name %></h1>
  <h2 class="mb-2 text-xl">Patients</h2>
  <ul>
    <% @doctor.patients.each do |patient| %>
      <li><%= patient.name %></li>
    <% end %>
  </ul>
</div>
